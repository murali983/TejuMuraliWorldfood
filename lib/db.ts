// Prisma-backed persistence is optional in this scaffold.
// The current public site uses static content, so we keep a null stub here
// until a database adapter and generated Prisma client are configured.
export const prisma = null;
